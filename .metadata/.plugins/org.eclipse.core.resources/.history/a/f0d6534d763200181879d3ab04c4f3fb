<?php


$conn = new mysqli("localhost", "admin","myHarvest", "password");
if($db->connect_errno){
    echo "DB connection failed. ".$DB->connect_error;
}

$USERNAME = "NKOHN";
$PASSWORD = "nkpass";
$CUSTOMER_ID = "7187445278";

$sql = "INSERT INTO AUTHORIZED_USERS(USERNAME, PASSWORD) 
VALUES($USERNAME, $PASSWORD)";

$stmt = $db->prepare($sql);

if($db->query($sql)=== true){
    echo "record created successfully";
}
    else{
        
    echo "error: ".$db->error;
   
}

$db->close();

?>